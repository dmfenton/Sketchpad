# Copy this to .env and fill in your values
# .env is gitignored

# Required: Anthropic API key
ANTHROPIC_API_KEY=sk-ant-...

# ECR Registry URL (from terraform output)
ECR_REGISTRY=573988763875.dkr.ecr.us-east-1.amazonaws.com/drawing-agent

# Image tag (usually 'latest' for production)
IMAGE_TAG=latest

# Database URL (SQLite)
DATABASE_URL=sqlite+aiosqlite:///data/drawing_agent.db

# Google OAuth (optional until auth is implemented)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_ID_IOS=
